<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nyle.demo.srtp_nyle_xyh" >
    <uses-permission android:name="android.permission.GET_TASKS"/>
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity android:name=".acitivity.MainActivity" android:label="MainActivity"></activity>
        <activity android:name=".acitivity.ProcessDetailActivity" android:label="ProcessDetailActivity"></activity>
        <activity android:name=".acitivity.TaskDetailActivity" android:label="TaskDetailActivity"></activity>
        <activity android:name=".acitivity.ServiceDetailActivity" android:label="ServiceDetailActivity"></activity>
        <activity android:name=".acitivity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
